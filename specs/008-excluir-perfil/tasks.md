# Tarefas: US-008 - Excluir Perfil

- [ ] Criar a pasta do perfil e os 3 arquivos markdown.
- [ ] Criar o arquivo `delete-profile.html` dentro da pasta `src/`.
- [ ] Montar a interface com um `<select>` para exibir os perfis e um botão de excluir.
- [ ] Criar o arquivo `delete-profile.js`.
- [ ] Fazer o JS ler o `localStorage` e preencher as opções do `<select>`.
- [ ] Implementar a exibição de um alerta de confirmação nativo (`confirm()`).
- [ ] Implementar a lógica de remoção do item no `localStorage` após confirmação.